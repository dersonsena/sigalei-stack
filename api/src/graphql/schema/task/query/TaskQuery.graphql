"Task queries"
type Query {
  """
  Get a task
  """
  task(id: String!): Task
  """
  Get all tasks
  """
  tasks(
    first: Int=10,
    after: String,
    last: Int,
    before: String
  ): TaskConnection
}
