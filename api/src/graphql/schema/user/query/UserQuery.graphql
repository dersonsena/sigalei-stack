"User Queries"
type Query {
  """
  Get a user
  """
  user(id: String!): User
  """
  Get a user
  """
  users(
    first: Int=10,
    after: String,
    last: Int,
    before: String
  ): UserConnection
  """
  Get user credentials
  """
  login(email: String!, password: String!): AuthToken
}
